// Build-time configuration. The UI is paired with one backend; set its URL here
// (e.g. your home-lab address) — no runtime settings screen.
export const environment = {
  apiBaseUrl: 'http://localhost:1212',
};
