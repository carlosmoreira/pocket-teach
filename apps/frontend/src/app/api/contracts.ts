// The frontend's view of the shared wire contract. Single source of truth lives
// in @pocket-teach/api-types; re-exported here as the app's import point.
export type * from '@pocket-teach/api-types';
